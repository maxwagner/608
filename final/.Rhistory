# read in the files
for (file in files) {
temp <- read.csv(file, stringsAsFactors = FALSE)
temp <- temp[c(2,3,5,8,9)]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
files = list.files(path = "data", pattern = "*.csv")
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
temp <- read.csv(file, stringsAsFactors = FALSE)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c(2,3,5,8,9)]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c(2,3,5,8,9)]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
head(temp)
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
head(temp)
temp <- temp[c("RANK")]
head(temp)
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
head(temp)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
summary(which(rec$Event == "100m"))
summary(which(recs$Event == "100m"))
table(which(recs$Event == "100m"))
which(recs$Event == "100m")
recs[which(recs$Event == "100m"),]
recs[which(recs$Event == "100m"),]
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
head(temp)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
recs[which(recs$Event == "100m"),]
recs[which(recs$Event == "10000m"),]
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
temp$MARK <- gsub(" ", "", temp$MARK)
recs[which(recs$Event == "100m"),]
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
temp$MARK <- gsub(" ", "", temp$MARK)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub(" ", "", temp$MARK)
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
recs[which(recs$Event == "100m"),]
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub(" ", "", temp$MARK)
temp
temp$MARK <- gsub("[[:space:]]", "", temp$MARK)
temp
temp$MARK <- gsub("[a-zA-Z]", "", temp$MARK)
temp
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub("[[:space:]]", "", temp$MARK)
# trim letters from marks
temp$MARK <- gsub("[a-zA-Z]", "", temp$MARK)
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
recs[which(recs$Event == "100m"),]
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub("[[:space:]]", "", temp$MARK)
# trim letters from marks
temp$MARK <- gsub("[a-zA-Z]", "", temp$MARK)
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
recs[which(recs$Event == "200m"),]
recs[which(recs$Event == "400m"),]
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub("[[:space:]]", "", temp$MARK)
# trim letters from marks
temp$MARK <- gsub("[a-zA-Z]", "", temp$MARK)
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
records <- read.csv("records.csv")
records <- read.csv("data/records.csv")
head(records)
head(recs)
records$Date <- as.Date(records$Date, "%d-%b-%y")
records <- read.csv("data/records.csv")
head(records)
# select all files that are csv
files = list.files(path = "data", pattern = "*.csv")
# data frame for collecting the files at the end
recs = data.frame()
# read in the files
for (file in files) {
temp <- read.csv(paste("data/", file, sep = ""), stringsAsFactors = FALSE)
temp <- temp[c("MARK", "COMPETITOR", "NAT", "VENUE", "DATE")]
# only keep the top 100 times for easier management
if (nrow(temp) > 100) {
temp <- temp[1:100,]
}
# trim whitespace from marks
temp$MARK <- gsub("[[:space:]]", "", temp$MARK)
# trim letters from marks
temp$MARK <- gsub("[a-zA-Z]", "", temp$MARK)
# give athletes capital names
library(stringi)
temp$COMPETITOR <- stri_trans_totitle(temp$COMPETITOR)
# fix nationality
temp$NAT <- substr(temp$NAT, 1, 3)
# fix date
temp$DATE <- as.Date(temp$DATE, "%d-%b-%y")
# add a sex column
if (grepl("_men", file)) {
temp$Sex <- "male"
} else if (grepl("_women", file)) {
temp$Sex <- "female"
} else {
temp$Sex <- "unknown"
}
# add an event column
event <- gsub("_.*$", "", file)
temp$Event <- event
# rename the columns
colnames(temp) <- c("Mark", "Athlete", "Nation", "Venue",
"Date", "Sex", "Event")
# rbind to the recs data frame
recs <- rbind(recs, temp)
}
head(recs)
table(recs$Event)
table(recs$Sex)
table(recs$Nation)
records <- read.csv("data/records.csv")
head(records)
head(recs)
records$Date <- as.Date(records$Date, "%m/%d/%y")
head(records)
records <- read.csv("data/records.csv")
head(records)
records$Date <- as.Date(records$Date, "%m/%d/%Y")
head(records)
table(records$Event)
records <- read.csv("data/records.csv")
head(records)
records$Date <- as.Date(records$Date, "%m/%d/%Y")
head(records)
table(records$Event)
table(records$Sex)
write.csv(records, "data/records.csv")
records$Date <- as.Date(records$Date, "%m/%d/%Y")
head(records)
table(records$Event)
table(records$Sex)
write.csv(records, "data/records.csv")
write.csv(records, "data/records.csv")
records <- read.csv("data/records.csv")
head(records)
records$Date <- as.Date(records$Date, "%m/%d/%Y")
head(records)
records <- read.csv("data/records.csv")
records <- read.csv("data/records.csv")
head(records)
records[which(Event == "100m"),]
records[which("Event" == "100m"),]
records[which(records$Event == "100m"),]
shiny::runApp()
